package exit.services.util;

public class Timer {
	
	private static Timer instance;
	
	private Timer(){
		
	}
	
	public synchronized static Timer getInstance(){
		if(null == instance){
			instance = new Timer();
		}
		return instance;
	}
	
	
	
}
